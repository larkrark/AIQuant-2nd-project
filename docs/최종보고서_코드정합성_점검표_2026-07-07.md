# 최종보고서 ↔ 코드/문서 정합성 점검표 (2026-07-07)

`main_final_hsi_overlay_report_draft.pdf`(팀 최종보고서)와 현재 저장소의 코드(`src/pipeline`,`src/common`)·
문서를 대조한 표. 판정: ✅정합 · 🟡부분(값은 맞으나 코드 미완/미세차) · 🔴미구현/불일치.

## 0. 요약
- ✅ **핵심 로직 대부분 정합** — λ 부분조정식, Turnover, 비용차감, t→t+1, dynamic_v1 규칙, MacroRisk, factor loading, 유니버스·BM.
- ✅ **(A) adoption_decision 비열등 판정 구현 완료**(2026-07-07) — `stage_selection.adoption_decision`, self-test 연결·테스트 통과.
- ✅ **(B) 비대칭 λ(asym) 구현 완료** — `stage_d_lambda.apply_asymmetric_lambda`, self-test에 asym_up0.1_down0.3/0.5/asym_up0.2_down0.3 반영.
- ✅ **(C) IS/OOS 분리 + walk-forward 구현 완료** — `pipeline/validation.py`(split_is_oos·walk_forward), self-test에 8구간 성과 산출.
- 🎉 **정합 갭 전부 해결.**
- 🟡 상태별 목표비중 규칙은 값은 일치하나 `stage_b_hsi` 스텁, 팩터 정의 미세차, self-test는 surrogate 수치.

---

## 1. 전략 구조·설계

| 영역 | 보고서 | 우리 코드/문서 | 판정 |
|---|---|---|---|
| 전략 철학(예측 아님, 상태해석+λ속도) | §1,3,14 | stage_d_lambda docstring, 통합정리 문서 | ✅ |
| λ 부분조정식 `w_t=w_{t-1}+λ(target−w_{t-1})` | §3 | `apply_lambda_partial_adjustment` | ✅ |
| 상태별 목표비중(risk_relief 70/20/10 … accident 0/30/70) | 표1 | 통합정리 문서 §2에 동일 기재. **코드 `stage_b_hsi.baseline_target_weights`는 스텁** | 🟡 값 일치·코드 미구현 |
| 유니버스 3종(069500/114260/153130) | 표2 | `common.config.ASSETS` | ✅ |
| BM Fixed 70/20/10 + EW | 표3 | `pipeline.config.FIXED_BM_WEIGHTS`, `stage_g_benchmark` | ✅ |
| SAA/TAA/λ 3층 | §5 | 문서·구조로 반영(SAA=중립/Fixed, TAA=상태 w*, λ=집행속도) | ✅ |

## 2. 데이터·백테스트 규율

| 영역 | 보고서 | 우리 코드 | 판정 |
|---|---|---|---|
| t월 신호 → t+1월 수익률(look-ahead 차단) | §6 | `common.backtest.align_weights_with_next_returns` | ✅ |
| 가격-수익률 재현성(최대오차 5e-7) | §6 | self-test 검증 게이트(비중합·정렬) | ✅ 방식 동일 |
| Turnover = 0.5·Σ|Δw| | 용어 | `common.backtest.calculate_turnover` | ✅ |
| 비용차감 = gross − Turnover×비용률 | §10 | `apply_transaction_cost` | ✅ |
| 비용 시나리오 0/5/10/20bp | 표4 | `pipeline.config.COST_RATE_GRID` | ✅ |
| Score식 폐기(사후 가중치 위험) | §7 | 방침 일치. **단 `stage_selection`은 아직 `SELECTION_SCORE_WEIGHTS` 사용** | 🔴 §4-A 참조 |
| IS(2012-04~2020-12)/OOS(2021-01~2026-06) 분리 | §9 | `validation.split_is_oos` + `config.IS_END/OOS_START` | ✅ |
| Walk-forward(60→12 rolling) | 표4 | `validation.walk_forward_*`, self-test 8구간 | ✅ |

## 3. Dynamic λ · MacroRisk · Factor loading

| 영역 | 보고서 | 우리 코드 | 판정 |
|---|---|---|---|
| dynamic_v1 규칙(기본 0.3·고위험 0.1·easing 0.5) | §12 | `rule_based_dynamic_lambda`(동일 값·조건) | ✅ |
| 고위험 조건(vol_z>1 / dd<−10% / macro≥2) | §12 | `DYNAMIC_LAMBDA_RULE` 동일 | ✅ |
| MacroRisk = rate_up+fx_up ≥ 2 | §12 | `macro_risk_high=2`. **단 외부 금리·환율 미보유 → self-test는 macro=0(=dynamic_v1)** | 🟡 규칙 일치·데이터 대기 |
| E24 factor loading(Market/Bond/Momentum/Volatility/MacroRisk, β·설명용, 회귀 사후선택 금지) | §11 | `stage_factor`(β·t·VIF·36m rolling) | ✅ |
| 팩터 정의(Volatility=069500 12m rolling 연변동성, Momentum=3m 누적) | §11 | `factor_ingest`에 `realized_vol`(월 실현변동성), `mom_3m/12m` 존재 — 정의 미세차 | 🟡 개념 동일·산식 미세차 |
| rolling factor exposure(36개월) | 그림10 | `stage_factor.rolling_loading(window=36)` | ✅ |
| 성과 attribution(SAA/타이밍/λ/비용) | 보고서엔 명시적 분해 약함 | `stage_attribution`으로 **추가 구현**(RA 요건 5c 보강) | ✅ 우리가 더 갖춤 |

## 4. Adoption decision (최종 판정) — 최대 갭

보고서 §13·표7·표8: **Score 아님, 사전등록 비열등 4조건 AND 통과**(OOS 10bp net).

| # | 비열등 조건(보고서 표7) | 우리 코드 |
|---|---|---|
| ① | Calmar_net ≥ 대칭 최우수 × 0.90 | ✅ `adoption_decision` cond1 |
| ② | MDD 악화 ≤ 대칭 λ=0.1 대비 2.0%p | ✅ cond2 |
| ③ | tail-month 평균수익 악화 ≤ 0.3%p | ✅ cond3 |
| ④ | 평균 Turnover ≤ 대칭 λ=0.3 × 1.5 | ✅ cond4 |

→ **✅ 해결(2026-07-07)**: `stage_selection.adoption_decision`가 4조건을 판정하고, `data_bridge`의 `oos_adoption_table`이 OOS 10bp net 지표(Calmar_net·MDD·tail-month·Turnover)를 계산해 self-test에 연결됨. self-test(surrogate)에서 dynamic_v1 non_inferior=True 확인. Score(`SELECTION_SCORE_WEIGHTS`)는 참고 지표로 병기 유지.

## 5. 후보군 커버리지

| 후보 | 보고서 | 우리 코드 |
|---|---|---|
| EW · Fixed BM · HSI baseline · λ=0.1 · λ=0.3 | 있음 | ✅ (self-test 후보) |
| dynamic_v1 | 최종 기본 후보 | ✅ (`rule_based_dynamic_lambda`) |
| dynamic_v1_macro | 저회전 확장안 | 🟡 규칙 있음·macro 데이터 대기 |
| asym_up/down (비대칭 λ) | asym_up0.1_down0.3 등 | ✅ `apply_asymmetric_lambda`(3종 self-test) |

## 6. 성과 수치 정합

- 보고서 FULL: FixedBM 10.99%/−25.67%, λ0.1 8.64%/−14.74%, λ0.3 9.09%/−15.22%, dynamic_v1 9.73%/−12.63%(Calmar 0.77).
- 우리 self-test 수치는 **surrogate(legacy baseline)** 기반이라 위와 다름(설계상 정상). 조원의 canonical baseline·수익률이 연결되면 재현 검증 가능.
- ⚠️ 버전 드리프트: 강의요건 문서가 지적한 16/17번 0.07~0.08%p 차이는 이 최종보고서에서 10/20~23 계열 수치로 정리된 것으로 보임(예: λ0.1 8.64, λ0.3 9.09) → convention 통일 확인 필요.

---

## 7. 결론 및 정합 조치 우선순위

**전체적으로 우리 코드·문서는 최종보고서 방법론과 방향·핵심 로직이 일치**한다. 남은 정합 작업 3가지:

| 순위 | 갭 | 조치 |
|---|---|---|
| ~~1~~ ✅ | adoption_decision(비열등 4조건) | **완료** — `stage_selection.adoption_decision` + `oos_adoption_table`, self-test 연결·7개 테스트 통과 |
| ~~2~~ ✅ | 비대칭 λ(asym_up/down) | **완료** — `apply_asymmetric_lambda`, self-test·테스트 반영 |
| ~~3~~ ✅ | IS/OOS 분리·walk-forward | **완료** — `pipeline/validation.py`, self-test 연결 |
| 4(보조) | `stage_b_hsi` 목표비중 규칙 구현 | 표1 값으로 `baseline_target_weights` 채우기(현재 surrogate 대체 중) |

**갭 1~3 전부 해결(2026-07-07).** 남은 것은 보조 항목 4(`stage_b_hsi` 목표비중 규칙 구현, 현재 surrogate 대체 중)뿐이며, 이는 조원 canonical baseline이 오면 자연 해소된다.
